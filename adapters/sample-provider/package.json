{
    "name": "@ghost-engine/sample-provider",
    "version": "0.1.0",
    "private": false,
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "clean": "rm -rf dist",
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run",
        "lint": "eslint .",
        "fmt": "prettier -w ."
    },
    "dependencies": {
        "@ghost-engine/kernel": "workspace:*",
        "@ghost-engine/protocol": "workspace:*"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "eslint": "^9.0.0",
        "prettier": "^3.0.0",
        "typescript": "^5.5.0",
        "vitest": "^2.0.0"
    }
}