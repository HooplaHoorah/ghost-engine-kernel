# MCP Adapters

This package houses adapters and interfaces for connecting Ghost Engine microservices to external systems and the microservice control plane (MCP). It abstracts underlying protocols and provides unified connectors for orchestrator and runtime services.
