name: Deploy Orchestrator to Cloud Run

on:
  push:
    branches: [main]
    paths: ['services/orchestrator/**']
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    env:
      GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
      GCP_WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
      GCP_SERVICE_ACCOUNT: ${{ secrets.GCP_SERVICE_ACCOUNT }}
      GCP_REGION: ${{ vars.GCP_REGION }}
      SERVICE_NAME: ${{ vars.ORCH_SERVICE }}
      IMAGE: ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/cloud-run-source-deploy/orchestrator:${{ github.sha }}
      RUNTIME_SA: cloud-run-runtime@${{ secrets.GCP_PROJECT_ID }}.iam.gserviceaccount.com

    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.GCP_SERVICE_ACCOUNT }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Enable APIs (idempotent)
        run: |
          gcloud services enable run.googleapis.com cloudbuild.googleapis.com artifactregistry.googleapis.com \
            --project="${GCP_PROJECT_ID}" --quiet

      # Build the Dockerfile explicitly and push to Artifact Registry
      - name: Build & Push (Cloud Build → AR)
        working-directory: services/orchestrator
        run: |
          gcloud builds submit . \
            --project="${GCP_PROJECT_ID}" \
            --tag="${IMAGE}" \
            --gcs-source-staging-dir="gs://$GCP_PROJECT_ID_cloudbuild/source" \
            --quiet

      - name: Deploy to Cloud Run (from image)
        run: |
          gcloud run deploy "${SERVICE_NAME}" \
            --image="${IMAGE}" \
            --project="${GCP_PROJECT_ID}" \
            --region="${GCP_REGION}" \
            --platform=managed \
            --service-account="${RUNTIME_SA}" \
            --allow-unauthenticated \
            --quiet

      - id: url
        run: echo "url=$(gcloud run services describe ${SERVICE_NAME} --project=${GCP_PROJECT_ID} --region=${GCP_REGION} --format='value(status.url)')" >> "$GITHUB_OUTPUT"

      - name: Smoke: /health CORS
        run: curl -I "${{ steps.url.outputs.url }}/health" | grep -q "Access-Control-Allow-Origin"

      - name: Smoke: /status HTML
        run: curl "${{ steps.url.outputs.url }}/status" | grep -q "<html>"
