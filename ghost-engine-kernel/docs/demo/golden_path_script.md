# Golden Path Demo Script (deterministic)

## Pre-flight
- Verify `/status` shows green services
- Load seeded project: `GE_DOOM_DEMO_01`

## Live clicks
1) Run Demo
2) Create Scene (3 entities)
3) AI Assist: Generate Variant (cache-first + fallback)
4) Build Replay (shareable artifact)

## Failure plan
- Use Cached Variant toggle
- Local docker-compose fallback
- Pre-recorded demo video fallback
