name: Deploy (AWS placeholder)
on: { workflow_dispatch: {} }
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: OIDC auth, ECR push, ECS deploy, CloudFront invalidation"
